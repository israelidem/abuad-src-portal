{
  "name": "abuad-src-portal",
  "version": "2.0.0",
  "private": true,
  "description": "ABUAD SRC Portal — student feedback and complaint tracking",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",
    "check:db": "npm --prefix backend run check:db",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "smoke": "npm --prefix backend run smoke",
    "test": "npm --prefix backend test",
    "verify": "npm run test && npm run lint && npm run build"
  }
}
